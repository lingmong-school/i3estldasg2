using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlayerInventory : MonoBehaviour
{
    public bool hasKeycard = false; // Tracks if the player has the keycard
    public bool hasPower = false; // Tracks if the player has the powercore
    public bool hasHull = false; // Tracks if the player has the hull
    public bool hasEngine = false; // Tracks if the player has the Engine
}